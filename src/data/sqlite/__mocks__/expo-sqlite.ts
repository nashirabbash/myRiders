// Minimal stub — only needed so module resolution doesn't fail in Node tests.
// Integration tests against a real SQLiteDatabase run on-device / in Expo Go.
export const openDatabaseAsync = jest.fn();
